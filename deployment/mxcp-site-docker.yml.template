mxcp: 1
project: {{PROJECT_NAME}}-mxcp
profile: prod
dbt:
  enabled: true
sql_tools:
  enabled: true
# Uncomment and list secrets when your tools need them:
# secrets:
#   - example-secret
profiles:
  prod:
    duckdb:
      path: /app/data/db-prod.duckdb
    audit:
      enabled: true
      path: /app/logs/audit.jsonl
